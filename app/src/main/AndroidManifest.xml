<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.worktimemanager">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HolidayAdder"
            android:screenOrientation="unspecified"
            android:theme="@style/popupTheme" />
        <activity
            android:name=".YearAndMonthSelector"
            android:screenOrientation="unspecified"
            android:theme="@style/popupTheme" />
        <activity android:name=".HolidayManagement" />
        <activity android:name=".DayoffViewer" />
        <activity
            android:name=".ScheduleAdder"
            android:screenOrientation="unspecified"
            android:theme="@style/popupTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>